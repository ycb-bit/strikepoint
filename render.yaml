# Render blueprint — deploy the whole stack with one "New +" → "Blueprint" click.
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: strikepoint
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
